{
  "name": "@mote/local-inference",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-manifest.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@mote/shared": "0.1.0"
  }
}
