/** Compatibility export: the evidence store is the only observation/version writer. */
export {EvidenceStore, EvidenceStore as Store, StoreError, sha256} from './evidence-store.js';
export type {Range} from './evidence-store.js';
