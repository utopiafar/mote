# 原生客户端导航与设置

Android 和 macOS 采集端采用同一套信息层级：日常使用、采集配置、开发诊断分开。界面沿用 Mote 的米白背景与深绿色强调色，以清楚的标题、留白、圆角分组和少量主操作组织内容；详细解释靠近对应设置。

## 组织原则

- 概览回答采集是否运行、记录是否等待同步，以及下一步需要处理什么。开始与停止采集保留在这里。
- 随手记拥有独立页面。切换页面不会提交或清除草稿。
- 来源集中管理日历与文件；进入页面不等于授权，连接动作仍由用户主动触发。
- 设置先显示分类入口，再进入连接、采集、隐私、权限或关于等具体页面。
- 开发者选项承载数值诊断、支持包、环境目录、模型推理参数和调试接口。它们不占用首页。
- 统计与结果详情作为概览的下级页面。采集端保留设备本地状态，长期资料浏览继续使用中央仓库。

macOS 使用常驻侧栏：概览、随手记、本地来源；设置位于侧栏底部。设置包含连接与设备、上传与同步、采集与存储、隐私与过滤、关于 Mote，以及单独分组的开发者选项。应用菜单支持 `⌘,` 打开设置，`⌘1`、`⌘2`、`⌘3` 切换前三个目的地，菜单栏图标也提供随手记与设置入口。

Android 使用概览、随手记、来源、设置四个底部目的地。设置包含连接与同步、采集与存储、隐私与应用规则、权限与后台运行、关于与更新；开发者选项位于关于与更新内，诊断与支持、模型高级设置再分到下级页面。隐私页面同时提供模型高级设置的快捷入口。详情提供返回入口，并保留系统返回行为。两端使用相同概念，具体控件遵循平台习惯。

这些设计取舍参考 [Android 导航模式](https://developer.android.com/design/ui/mobile/guides/layout-and-content/layout-and-nav-patterns)、[Android 设置设计](https://developer.android.com/design/ui/mobile/guides/patterns/settings)、[Apple 侧栏](https://developer.apple.com/design/human-interface-guidelines/sidebars)与 [Apple 设置](https://developer.apple.com/design/human-interface-guidelines/settings)。其中 Android 文档明确建议将大量设置分组到下级页面，并把常用操作放在对应功能附近。

## 验收清单

以下是回归验收要求，不是已经执行的测试记录。构建、合成 fixture、模拟器、真机与真实模型的结果应分别报告。

| 范围 | 应有行为 |
| --- | --- |
| 首次打开 | 默认停留概览；未配置连接时能直接进入连接设置；不会自动开始采集、请求相机或日历权限。 |
| 主导航 | 一次只显示一个目的地；选中项清楚；切换后标题与返回位置明确。 |
| 页面状态 | 未保存设置与随手记输入在页面切换后保留；恢复设置不清空已保存笔记或队列。 |
| 设置保存 | 所有分类使用同一份配置；无效输入定位到所属页面；运行中保持原有配置修改限制。 |
| 连接返回 | 扫码或邀请成功后显示新节点与设备名称；手工改为另一节点仍清除旧凭据，队列目的地保护保留。 |
| 随手记 | 编辑、离线保存、失败重试与页面重建恢复保持原有行为；隐藏页面不重复绑定草稿监听。 |
| 隐私 | 应用分级、排除、遮罩和模型设置完整保留；安全窗口与权限边界保持原有行为。 |
| 来源 | 文件、目录与日历入口可用；编辑现有来源时展现对应设置；进入页面不自动授权。 |
| 开发者选项 | 普通设置可以找到入口；诊断导出、模型参数、调试接口与环境信息可达。 |
| 更新 | 关于页可达；检查、下载、安装仍遵循现有状态约束；安装前保护尚未保存的输入。 |
| 桌面键盘 | `Tab` 不进入隐藏页面，当前导航有可访问名称；`⌘,` 打开设置，错误字段可被聚焦。 |
| 窗口与字体 | 最小桌面窗口、手机窄屏、系统大字体与软键盘弹出时，标题、操作和导航不被裁切或遮住。 |

自动化界面验证只使用新建隔离配置和合成内容；截图只包含应用生成的 fixture 界面。未获得明确授权时，不对个人设备进行真实采集，也不使用个人屏幕作为测试材料。

## 首轮导航改版验证（2026-09-14）

- macOS：完整桌面构建（TypeScript、Swift 与 C++ 助手、图标）通过，144 项桌面单元测试通过。
- Electron 隔离 UI fixture：主导航、原生设置菜单、配置锁定时的导航、跨页草稿与设置保留、隐藏错误字段定位、还原修改、来源编辑、配置与离线笔记保存，以及 820 × 620 最小窗口检查通过。
- Electron 随手记 fixture：输入法组合阶段、中文和 emoji、超长输入提示、失败后保留输入与修正后保存通过。
- Electron 连接 fixture：JSON/链接/二维码邀请、失败后配置保留、显式地址确认、队列节点保护、同节点重新授权，以及连接后保留其他分类的未保存修改通过。

上述桌面 fixture 始终保持采集停止；连接测试使用本机合成服务和测试密钥存储适配器。界面截图位于 `apps/desktop/release/*ui-fixture.png`。本次未做物理设备采集或真实模型调用验证。

- Android：development Kotlin 编译、开发 APK 与 instrumentation APK 构建通过，11 个 JVM 测试套件的 49 项测试通过。
- Android API 35 专用 fixture 模拟器：3 项 instrumentation 检查通过，覆盖配置与随手记分离、跨页输入与旋转恢复、隐藏无效字段定位、安全窗口标记、加密草稿重建恢复，以及四个主页面的生成界面渲染。模拟器以 `-read-only -no-snapshot` 启动，退出后丢弃临时变更。
- Android 界面预览位于 `apps/android/app/build/reports/navigation-ui/{overview,notes,sources,settings}.png`。它们由重置后的 fixture 应用使用自身 `View.draw(Canvas)` 生成；产品 `FLAG_SECURE` 保持开启，未获取设备屏幕截图、未授予采集权限、未调用真实模型。


## 配置与同步扩展

本地采集前置条件、四种上传方式、首次积存记录归属、用户友好配置控件与平台调度边界见 [采集与上传说明](collection-and-sync.md)。概览分开呈现采集和同步；服务端使用最后一次收到的设备状态，不把长时间未上报直接等同于已停止。

## 配置与同步扩展验证（2026-09-14）

- 整个 npm 工作区类型检查与生产构建通过，涵盖共享协议、服务端、Web、Mac TypeScript、Swift / C++ 助手和图标。
- 桌面最终完整回归 **164 项**通过，包含超限心跳聚合、暂停来源的等待状态与显式同步收尾心跳。本机显示真实待传总量，心跳数量按协议上限发送。
- Electron UI、中文输入和连接 fixture 通过：无 URL / 令牌时可请求本地采集（采集 IPC 为测试桩）、四种上传选择、预设保存、合成应用选择器、遮挡滑块、首次积存归属确认、合并笔记与来源待传数量、跨页输入保留和最小窗口布局。
- 桌面真实端到端 `apps/desktop/scripts/offline-sync-smoke.cjs` 通过：Electron preload / main IPC → 临时真实 Mote 服务端与 SQLite。无 URL 时先保存合成笔记和文件，首绑必须确认，手动模式零自动请求，明确上传后两类原始记录获得真实 ACK 并清空待传；中央最终上报为 `manual / idle / 0`。有新待传时更换节点被拒绝且原内容保持。测试阻止真实屏幕采集，并以测试适配器替换 Keychain。
- 共享协议 **16 项**、服务端 API / 元数据 / 连接 / 配置与同步心跳集成 **35 项**通过，覆盖状态保存与重启恢复、旧客户端兼容、参数边界和认证。
- 中央 Web **20 项单测**、生产构建及导航、来源、连接授权、元数据、诊断 **5 组真实渲染 fixture** 通过。覆盖配置分类、预设和来源勾选、切页保留草稿、`.env` 下载字面值与密钥隔离、明确不热更新、采集/同步状态分离、文件上传两种保留方式和窄屏布局。文件选择在清空输入框前保留 `FileList`，避免异步导入丢失文件。
- Android development 构建、**60 项 JVM** 与 **14 项 instrumentation** 全部通过，0 失败、0 跳过。其中 10 项覆盖无 URL 加密笔记与生成活动采集、首次绑定和目的地保护、四种策略、来源元数据注册、手动失败不自动重试、错误 ACK / 断连留存及 25 条分块最终 idle；另有 3 项导航 / 应用选择器 / 生成预览检查和 1 项加密草稿重建。专用 API 35 模拟器以只读、无快照方式运行，测试后已退出；原 AVD 未删改。

上述测试均使用合成资料与隔离环境，未执行物理设备采集、真实个人屏幕或真实模型验证。Android 产品 `FLAG_SECURE` 保持开启，预览由应用自身 `View.draw(Canvas)` 生成。

| 预览 | 位置 |
| --- | --- |
| Mac 概览、上传设置、采集预设、遮挡编辑 | `apps/desktop/release/{ui-fixture,sync-ui-fixture,capture-settings-ui-fixture,mask-editor-ui-fixture}.png` |
| 中央首页、分类设置、设备与窄屏布局 | `.mote/web-navigation/` |
| Android 四个主页面与三个配置页 | `apps/android/app/build/reports/navigation-ui/{overview,notes,sources,settings,sync-settings,capture-settings,privacy-settings}.png` |

具体调度与目标绑定边界见 [统一行为说明](collection-and-sync.md) 和 [Android 说明](android.md)。

## 0.8.0 发布前复验

- 全工作区 368 项测试、类型检查和 server/web 构建通过；隐私、端到端、MCP、更新与回滚、安全边界、独立配置、隧道及 8 项归档解析测试通过，模型响应均来自合成 fixture。
- 本地 0.8.0 Mac 安装包通过签名、架构、版本、包内依赖与生成旧字段配置/队列/草稿迁移检查；未启动日常 App 或访问 Keychain。公开产物由发布流水线重新构建。
- Android 0.8.0 / versionCode 11 的 60 项 Debug JVM 测试通过；Release lint 为 0 错误 / 105 警告，Development lint 为 0 错误 / 118 警告。Release 编译与原生库合并、Development APK 构建通过；本地检查显式移除分发签名变量，正式 Release 签名打包交由 CI。
