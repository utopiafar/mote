# Mote 0.0.49

本版修复了 source-sync v2 后远端发布 pre-case 中仍读取旧版 `pending` 队列，以及文件证据版本变更 fixture 的时序断言问题。

- 连接、来源同步和文件证据 pre-case 兼容 `pendingRealtime` / `pendingHistory`，并覆盖持久化版本失效。
- 保留 MCP 上下文查询重构、只读工具契约和生成数据验收；模型验收使用本机 Codex Server，未调用 DeepSeek。
- 所有发布前例只使用生成数据；本次未执行物理设备采集。
